import PuppeteerHandler from './PuppeteerHandler';

class TwitterPageHandler {
  constructor () {

  }

  async login() {
    /*
    const url = 'https://twitter.com/';

    const puppeteer = new PuppeteerHandler();
  	await puppeteer.launch()

    const page = await browser.newPage();

    // パスワードを使ってログイン
    await page.goto(LOGIN_URL, {waitUntil: 'domcontentloaded'});
    await page.type('input[name="user[email]"]', ACCOUNT_EMAIL);
    await page.type('input[name="user[password]"]', ACCOUNT_PASS);
    page.click('input[name="commit"]');
    await page.waitForNavigation({timeout: 60000, waitUntil: 'domcontentloaded'});
    */
  }



}

export default TwitterPageHandler
