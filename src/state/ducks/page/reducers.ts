/*
State shape

page: {
  url: string,
  isFetching: boolean,
}
*/

const initialState = {
  url: '',
  isFetching : false,
}

const pageReducer = (state = initialState, action) => {
  return state
}

export default pageReducer
